# En CI (p. ej. Vercel) pnpm usa frozen-lockfile por defecto.
# Si el lockfile y package.json no coinciden en orden de claves, falla.
# Permitir install sin frozen-lockfile para que el build no falle.
frozen-lockfile=false
