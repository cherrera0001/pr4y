# Local SDK path (cada máquina tiene la suya)
local.properties
# Keystores (nunca subir al repo)
*.jks
keystore*
# Último versionCode publicado (cada dev/CI mantiene el suyo o usa -PlastReleasedVersionCode)
.last-release-versioncode

# Cache de Gradle
.gradle/
build/
