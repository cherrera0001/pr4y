<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Visibilidad de paquetes (Android 11+): necesario para Credential Manager / Google ID -->
    <queries>
        <package android:name="com.google.android.gms" />
    </queries>
    <!-- Red: API, Railway y comprobar conectividad -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Biometría: necesario para BiometricPrompt (auth con huella/face) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" android:maxSdkVersion="28" />

    <application
        android:name=".Pr4yApp"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pr4y"
        android:usesCleartextTraffic="true">

        <!-- Estabilización dev / MIUI: reducir intentos de optimización propietaria (LB/FrameInsert) -->
        <meta-data
            android:name="com.pr4y.app.MIUI_OPTIMIZATION_DISABLED"
            android:value="${miui_optimization}" />
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />
        <!-- Auditoría MIUI: desactivar motor MEMC / low-latency que generan FrameInsert/LB en logs -->
        <meta-data android:name="com.miui.sdk.display.extra_refresh_rate" android:value="true" />
        <meta-data android:name="miui.low_latency_mode" android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Pr4y">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
