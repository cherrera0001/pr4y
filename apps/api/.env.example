# PR4Y API - copy to .env and fill values

# Postgres (required for run + migrate).
# Local: URL estática (ej. postgresql://user:password@localhost:5432/pr4y).
# Railway: usar referencia dinámica, NO string estático → Variables → DATABASE_URL = ${{Postgres.DATABASE_URL}}
DATABASE_URL=postgresql://user:password@localhost:5432/pr4y

# JWT for access tokens (required; use a long random secret in prod)
JWT_SECRET=your-secure-jwt-secret-min-32-chars

# Google OAuth Web: ÚNICO Client ID usado por la API para validar id_token en POST /v1/auth/google.
# La API no debe usar nunca GOOGLE_ANDROID_CLIENT_ID para validación (solo la app Android lo usa localmente).
GOOGLE_WEB_CLIENT_ID=
# Opcional: flujos server-side; no se usa en verifyIdToken.
GOOGLE_WEB_CLIENT_SECRET=

# Solo referencia: GOOGLE_ANDROID_CLIENT_ID puede existir en Railway para la app Android; la API lo ignora.

# CORS: orígenes web permitidos, separados por comas. Ej: https://pr4y.cl,http://localhost:3000
# La app Android (com.pr4y.app / com.pr4y.app.dev) no envía Origin; esas peticiones se aceptan sin estar en esta lista.
CORS_ORIGINS=
