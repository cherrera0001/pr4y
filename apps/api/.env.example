# PR4Y API - copy to .env and fill values

# Postgres (required for run + migrate)
DATABASE_URL=postgresql://user:password@localhost:5432/pr4y

# JWT for access tokens (required; use a long random secret in prod)
JWT_SECRET=your-secure-jwt-secret-min-32-chars

# Allowed CORS origin (optional; default http://localhost:3000)
CORS_ORIGIN=http://localhost:3000
