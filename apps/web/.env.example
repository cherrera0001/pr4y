# API pública (Railway). Obligatoria: el frontend no tiene URL por defecto en código.
NEXT_PUBLIC_API_URL=

# Google OAuth Web: mismo valor que GOOGLE_WEB_CLIENT_ID del backend. Obligatorio para Sign in with Google.
NEXT_PUBLIC_GOOGLE_WEB_CLIENT_ID=

# Host canónico para redirección vercel.app → dominio (opcional). Ej: pr4y.cl
NEXT_PUBLIC_CANONICAL_HOST=

# Token de administrador: puerta /admin/gate. Opcional.
ADMIN_SECRET_KEY=
