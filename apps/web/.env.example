# API pública (Railway)
NEXT_PUBLIC_API_URL=https://pr4yapi-production.up.railway.app/v1

# Token de administrador: obligatorio para acceder a /admin (incl. login). Si no se define, /admin solo exige JWT admin.
ADMIN_SECRET_KEY=your-secret-here
